namespace Language_Integrated_Query;

class Program
{
    static void Main(string[] args)
    {
        SortingOperators.Run();
    }
}

// 필터(Filter): FilteringOperators
// 프로젝션(Projection): ProjectionOperators
// 집계(Aggregation): AggregationOperators
// 정렬(Sorting): SortingOperators
// 집합(Set): SetOperators
// 조인(Join): JoinOperators
// 요소(Element): ElementOperators
// 생성(Generation): GenerationOperators
// 변환(Conversion): ConversionOperators
// 양적(Quantifier): QuantifierOperators
// 그룹(Grouping): GroupingOperators